/**
 * Copy this file to `config.js` and paste your Supabase project URL + anon key.
 * Dashboard: Supabase → Project Settings → API.
 *
 * The anon key is safe to use in the browser because Row Level Security (RLS)
 * in the database decides what each user can read or write.
 */
window.HOMEEASE_SUPABASE_URL = "https://YOUR_PROJECT_REF.supabase.co";
window.HOMEEASE_SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
